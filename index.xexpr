;; -*- mode: scheme; indent-tabs-mode: nil -*-

(let* ()
  `(html
    (head (title "Github workflows"))
    (body
     "Hello World."
     (p
      "Hello again, here is a left: < and a right > and a slash-right />")
     (ul
      (li (a ((href "escape-codes.html")) "escape-codes"))
      (li (a ((href "signin.html")) "sign-in"))
      (li (a ((href "forking.html")) "make your own fork"))))))
